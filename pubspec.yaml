name: gps_inf
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'
  

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^11.1.0
  device_info: ^2.0.0
  http: ^1.1.0
  flutter_blue_plus: ^1.0.0
  permission_handler: ^10.2.0
  sensors_plus: ^4.0.2
  flutter_background: ^1.0.3
  shared_preferences: ^2.0.6
  flutter_charts : ^0.5.2
    # 高德Flutter定位插件
  amap_flutter_base: ^3.0.0 # flutter2空安全新版本
  amap_flutter_map: ^3.0.0 # flutter2空安全新版本
  amap_flutter_location: ^3.0.0 # flutter2空安全新版本
  coordtransform: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true